lua
string连接用 .. 不用 + 
+号用法比C#还要灵活的多 比如 "10" + 10  等于 20
相反 当lua期望一个string然而碰到数字 数字会被转为string
但是像不能转为数字的string 再 + 10 这种情况就报错

目前感觉lua比C#飘的多
C#是一种厚重又很精确的感觉
Lua的感觉就是 啊 都可以 很多非常规操作 飘渺一些 很多 挖草 这也可以

Userdata 我看到这里就没细讲 查了一下说的是表示在C中定义的类型
提供了一块原始的内存区域,可以用来储存任何东西
我的理解就是 引用类型 或者说 指针
后面用的时候再看吧

线程跳过 后面细讲

lua的逻辑运算符
and or not

表
Lua的数据结构
索引从1开始 wtf?
跟lua语言的特点一样 lua的表也是一个相当飘的东西
可以当数组用  a[2] 这样
也可以当字典用  
a = { x = 0 , y = 0 }  <-->  a = {}; a.x = 0 ; a.y = 0;
